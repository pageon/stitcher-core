directories:
    src: ./tests/src
    public: ./tests/public
    image: ./tests/public/img
    cache: ./tests/.cache
    template: ./tests/src/template_twig

engines:
    image: gd
    template: twig

meta:
    viewport: width=device-width, initial-scale=1

