directories:
    src: ./tests/src
    public: ./tests/public
    cache: ./tests/.cache

image:
    engine: gd
    dimensions:
        1x1: { width: 1, height: 1}

meta:
    viewport: width=device-width, initial-scale=1

engine: smarty
