directories:
    src: ./tests/src
    public: ./tests/public
    data: ./tests/src/data
    cache: ./tests/.cache

image:
    dimensions:
        1x1: { width: 1, height: 1}

meta:
    viewport: width=device-width, initial-scale=1
